{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "[ -e .env ] && set -a && . ./.env; next dev",
    "build": "next build",
    "start": "[ -e .env ] && set -a && . ./.env; next start",
    "lint": "next lint"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "cookies-next": "^2.1.2",
    "moment": "^2.29.4",
    "next": "13.4.9",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  }
}
